doctype html
html
  head
    title Editar Dato
  body
    h1 Editar Dato
    form(action=`/editar/${dato.id}` method="POST")
      if error
        p.error= error
      label(for="nuevoDato") Nuevo Dato:
      input(type="text" name="nuevoDato" value=dato.columna1)
      button(type="submit") Actualizar
    a(href="/") Volver a la lista
